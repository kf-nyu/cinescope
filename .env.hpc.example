# CineScope — NYU Dataproc / HDFS configuration
# Copy to .env on the Dataproc login node. Never commit .env.

CINESCOPE_STORAGE_BACKEND=hdfs
SPARK_MASTER=yarn

# Required: your NYU NetID only (lowercase letters/digits, no email domain).
# Example: if your email is ab1234@nyu.edu, set:
#   CINESCOPE_NETID=ab1234
# Scripts derive HDFS paths as:
#   hdfs:///user/${CINESCOPE_NETID}_nyu_edu/cinescope-data
CINESCOPE_NETID=

# Optional override if your HDFS home is not <netid>_nyu_edu:
# CINESCOPE_HDFS_USER=

# Leave these blank to auto-fill from CINESCOPE_NETID (recommended).
# CINESCOPE_DATA_ROOT=
# SPARK_WAREHOUSE_DIR=
# SPARK_CHECKPOINT_DIR=
# SPARK_LOCAL_DIR=
# CINESCOPE_DOWNLOAD_STAGING=

SPARK_DRIVER_MEMORY=4g
SPARK_SHUFFLE_PARTITIONS=200
CINESCOPE_MIN_FREE_GB=0
